apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizen-bff-adminpanel.fullname" . }}
  labels:
    {{- include "aizen-bff-adminpanel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizen-bff-adminpanel.selectorLabels" . | nindent 4 }}
