apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizen-bff-marine-mobile.fullname" . }}
  labels:
    {{- include "aizen-bff-marine-mobile.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizen-bff-marine-mobile.selectorLabels" . | nindent 4 }}
