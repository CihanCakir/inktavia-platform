apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizen-bff-marine.web.fullname" . }}
  labels:
    {{- include "aizen-bff-marine.web.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizen-bff-marine.web.selectorLabels" . | nindent 4 }}
