apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aizen-bff-marine.web.fullname" . }}-test-connection"
  labels:
    {{- include "aizen-bff-marine.web.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aizen-bff-marine.web.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
