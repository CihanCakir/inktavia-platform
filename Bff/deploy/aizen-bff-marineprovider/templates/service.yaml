apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizen-bff-marineprovider.fullname" . }}
  labels:
    {{- include "aizen-bff-marineprovider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizen-bff-marineprovider.selectorLabels" . | nindent 4 }}
