apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizen-content.fullname" . }}
  labels:
    {{- include "aizen-content.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizen-content.selectorLabels" . | nindent 4 }}
