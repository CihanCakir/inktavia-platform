apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aizen-content.fullname" . }}-test-connection"
  labels:
    {{- include "aizen-content.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aizen-content.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
