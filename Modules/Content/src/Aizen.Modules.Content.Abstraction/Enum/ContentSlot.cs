namespace Aizen.Modules.Content.Abstraction.Enum;

/// <summary>Placement slot within a surface (the "where exactly" axis, §3).</summary>
public enum ContentSlot
{
    HomeHero,
    Feed,
    Banner,
    Sidebar,
    DedicatedPage
}
