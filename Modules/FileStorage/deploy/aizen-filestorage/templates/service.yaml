apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizen-filestorage.fullname" . }}
  labels:
    {{- include "aizen-filestorage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizen-filestorage.selectorLabels" . | nindent 4 }}
