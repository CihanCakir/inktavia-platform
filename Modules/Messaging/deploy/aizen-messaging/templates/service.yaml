apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizen-messaging.fullname" . }}
  labels:
    {{- include "aizen-messaging.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizen-messaging.selectorLabels" . | nindent 4 }}
