apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aizen-messaging.fullname" . }}-test-connection"
  labels:
    {{- include "aizen-messaging.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aizen-messaging.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
