apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizen-payment.fullname" . }}
  labels:
    {{- include "aizen-payment.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizen-payment.selectorLabels" . | nindent 4 }}
