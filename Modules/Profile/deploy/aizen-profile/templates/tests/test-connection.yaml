apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aizen-profile.fullname" . }}-test-connection"
  labels:
    {{- include "aizen-profile.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aizen-profile.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
