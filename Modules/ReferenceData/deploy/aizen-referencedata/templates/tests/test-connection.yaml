apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aizen-referencedata.fullname" . }}-test-connection"
  labels:
    {{- include "aizen-referencedata.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aizen-referencedata.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
