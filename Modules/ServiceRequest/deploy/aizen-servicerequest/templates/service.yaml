apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizen-servicerequest.fullname" . }}
  labels:
    {{- include "aizen-servicerequest.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizen-servicerequest.selectorLabels" . | nindent 4 }}
