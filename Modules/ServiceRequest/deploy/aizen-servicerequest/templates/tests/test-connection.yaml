apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aizen-servicerequest.fullname" . }}-test-connection"
  labels:
    {{- include "aizen-servicerequest.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aizen-servicerequest.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
