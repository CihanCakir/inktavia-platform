apiVersion: v1
kind: Service
metadata:
  name: {{ include "aizen-vessel.fullname" . }}
  labels:
    {{- include "aizen-vessel.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aizen-vessel.selectorLabels" . | nindent 4 }}
