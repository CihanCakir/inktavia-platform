apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-provider.fullname" . }}
  labels:
    {{- include "web-provider.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-provider.selectorLabels" . | nindent 4 }}
