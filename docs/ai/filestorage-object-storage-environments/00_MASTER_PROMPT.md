# 00 - Master Prompt - FileStorage Object Storage Environments

You are working inside the Inktavia Marine OS backend repository.

Implement a complete object storage environment setup for the FileStorage module.

Scope:

- Local MinIO through Docker Compose.
- AWS S3 configuration for Dev/Test/Production.
- Consistent `S3ObjectStorage` options contract.
- Provider selection through configuration.
- Secure secret handling.
- Signed upload/read URL validation.
- FileStorage Postman documentation and tests.
- Build validation and final report.

Do not activate Payment or Profile flows.

Use the existing repository architecture. Do not create unrelated abstractions if equivalent AizenFramework or FileStorage abstractions already exist.

Execute the numbered prompts in order.
